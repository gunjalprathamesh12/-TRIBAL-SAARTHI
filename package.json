{
  "name": "tribal-saarthi",
  "version": "1.0.0",
  "description": "TRIBAL SAARTHI - AI-Enabled Scholarship & Fellowship Management System for Scheduled Tribes (SIH PS 26239)",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "seed": "cd server && node seed/seedRunner.js",
    "server": "cd server && node server.js",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
